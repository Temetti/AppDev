#pragma once
//Header file is usually composed by 3 parts
/* 
1. Constant of definitions(either using #define or enumeration)
2. Data structure definitions
3. Function declarations
*/

#define ROWS 12

//Function declarations here
int factorial(int n);
int binomial(int n, int k);